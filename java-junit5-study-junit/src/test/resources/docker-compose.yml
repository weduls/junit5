version: '3.1'

services:
  mysql:
    image: mariadb:latest
    restart: "always"
    ports:
    - "3306"
    environment:
      - MYSQL_ROOT_PASSWORD=dbsafer00
      - MYSQL_DATABASE=test
      - MYSQL_USER=wedul_dev
      - MYSQL_PASSWORD=dbsafer00